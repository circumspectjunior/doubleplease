export default function Disclaimer() {
  return (
    <footer className="disclaimer">
      <p>
        Decision-support only, not a guarantee. This ranks fixtures where the model disagrees
        with the market - a real signal, backtested against three seasons of results - not a
        promise of profit. See <code>build-plan.md</code> (Sections 1 &amp; 11) and{" "}
        <code>review.md</code> before acting on any of it, and only ever with money you can
        afford to lose.
      </p>
    </footer>
  );
}
